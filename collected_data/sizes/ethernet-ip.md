## A Note About EtherNet/IP

The EtherNet/IP tests done in the `captures` directory were completed using a firmware binary that is not based on ESP-32, using a closed-source implementation of EtherNet/IP. As such, no valid comparison can be made between RAM/Flash and the comparison is omitted.
